package com.mi.bms.shared.domain;

/**
 * Marker interface for value objects in the domain model.
 * Value objects are immutable and have no identity of their own.
 * They are defined by their attributes and are interchangeable if their
 * attributes are the same.
 */
public interface ValueObject {
    // No methods required - this is just a marker interface
}